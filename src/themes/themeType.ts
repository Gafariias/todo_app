export type themeTS = {
    title: string,
    colors: {
        background: string;
        font: string;
        elements: string;
        font_highlight: string;
        element_highlight: string
    },
    images: {
        desktop: string;
        mobile: string
    }
}