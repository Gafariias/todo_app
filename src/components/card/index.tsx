import { Container } from "./styles";

interface CardTS {
    title: string
    done: boolean
}

export default function Card(p: CardTS) {
    return(
        <Container>
            
        </Container>
    )
}